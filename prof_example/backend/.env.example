# Local: để trống DATABASE_URL thì tự dùng SQLite, chạy được ngay không cần cài Postgres.
# Khi lên Neon/Supabase thì dán connection string vào đây.
DATABASE_URL=
JWT_SECRET=doi-chuoi-nay-truoc-khi-deploy
JWT_EXPIRE_MINUTES=60
CORS_ORIGINS=http://localhost:5500,http://127.0.0.1:5500
